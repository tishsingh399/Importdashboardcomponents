
  # Import dashboard components

  This is a code bundle for Import dashboard components. The original project is available at https://www.figma.com/design/CcN5J2ewVdD33PcP1T0U4i/Import-dashboard-components.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  